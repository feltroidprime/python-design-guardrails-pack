[pytest]
# These tests exercise pack-owned scripts, not the application package.
# The root suite retains its application coverage gate; this local config keeps
# the focused acceptance command from reporting unrelated zero application coverage.
addopts = --no-cov --strict-config --strict-markers --disable-socket
pythonpath = ../../.. ../../../..
