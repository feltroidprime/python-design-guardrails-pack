"""The pack-owned foundation of the package.

`_foundation` holds the command seam, the machine envelopes and the outcome
table. The name starts with an underscore, so the ownership predicate answers
pack-owned for every file below it, and a pack update replaces each one whole.
"""
